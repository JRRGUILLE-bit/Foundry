name: A5.4 Run Ingwe Features Audit

on:
  push:
    branches:
      - agent/a5-4-ingwe-features

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run generator embedded in temporary workflow
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          source = Path('.github/workflows/a5-4-generate-ingwe-features.yml').read_text()
          start = source.index("          node <<'NODE'\n") + len("          node <<'NODE'\n")
          end = source.index("          NODE\n", start)
          lines = source[start:end].splitlines()
          Path('/tmp/a5-4-generate.js').write_text('\n'.join(line[10:] if line.startswith('          ') else line for line in lines) + '\n')
          PY
          node /tmp/a5-4-generate.js
      - name: Commit generated audit file
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add audit/features/ingwe.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "A5.4: generate Ingwe features and resources audit"
            git push
          fi
