name: Patch Browser QA Viewport

on:
  push:
    branches:
      - agent/a17-2-spell-localization
    paths:
      - ".github/workflows/patch-browser-qa-viewport.yml"

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/a17-2-spell-localization

      - name: Stabilize mobile breakpoint before first open
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path("audit/browser-mobile-qa-runner.mjs")
          source = path.read_text()
          old = '''async function openCharacter(page, profile, characterId) {
            const opened = await page.evaluate((id) => window.BANDA_MOBILE_SHELL.open(id), characterId);'''
          new = '''async function openCharacter(page, profile, characterId) {
            await page.waitForFunction(() => matchMedia("(max-width: 820px)").matches && innerWidth <= 820, null, { timeout: 5_000 });
            await sleep(350);
            const opened = await page.evaluate((id) => window.BANDA_MOBILE_SHELL.open(id), characterId);'''
          if old not in source:
              raise SystemExit("Expected openCharacter block not found")
          path.write_text(source.replace(old, new, 1))
          PY

      - name: Commit runner fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add audit/browser-mobile-qa-runner.mjs
          git commit -m "Stabilize emulated mobile viewport before opening sheets"
          git push origin HEAD:agent/a17-2-spell-localization